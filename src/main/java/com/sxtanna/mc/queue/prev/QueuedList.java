/*
package com.sxtanna.mc.queue.prev;

import java.util.Optional;
import java.util.UUID;

public interface QueuedList
{

	long IS_NOT_QUEUED = -1L;
	long EXPIRES_AFTER = +900_000L;


	int size();


	long getPosition(final UUID uuid);

	long setPosition(final UUID uuid, final long position);


	boolean vacant();

	boolean delete(final UUID uuid);


	Optional<UUID> next();

	Optional<UUID> last();


	void vacate();
}
*/
